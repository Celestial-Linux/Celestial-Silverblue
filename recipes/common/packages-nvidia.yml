# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: script
    scripts:
      - install_nvidia_cdi.sh
  - type: systemd
    system:
      enabled:
        - nvidia-cdi-refresh.path
        - nvidia-cdi-refresh.service
