type: dnf
source: local
repos:
    copr:
        - name: bieszczaders/kernel-cachyos-addons
          chroot: fedora-42-x86_64
install:
    packages:
        - cachyos-settings
        - scx-scheds
        - scx-manager
remove:
    packages:
        - zram-generator-defaults
